<%= form_for(@lab, url: 'building/lab/new', remote: true) do |f| %>
  <% if @lab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lab.errors.count, "error") %> prohibited this lab from being saved:</h2>

      <ul>
      <% @lab.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id="create_labolatory" class="input-group">
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: t('.name'), required: "required", class: "input-txt" %>
  </div>
  <div class="form-footer">
    <%= f.submit t('.create'), :class => "btn btn-primary btn-right" %>
<% end %>